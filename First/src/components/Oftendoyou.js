import React, { useState } from "react";
import { SafeAreaView, View, Text, TouchableHighlight, Image, TouchableOpacity, StyleSheet, ScrollView, TextInput, KeyboardAvoidingView } from "react-native";
// import Icon from 'react-native-vector-icons/FontAwesome';

const Oftendoyou = ({navigation}) => {
    return (
        <View style={styles.container}>
        
        </View>
    );
};

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#0F0829"
    },

});

export default Oftendoyou;